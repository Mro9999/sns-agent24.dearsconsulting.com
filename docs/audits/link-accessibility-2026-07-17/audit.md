# リンク・アクセシビリティ監査（2026-07-17）

## 結論

SNS Agent24 の主要導線、認証リダイレクト、ページ内リンク、キーボード操作、ランドマーク、見出し構造、リンク名・画像代替テキスト、フォーカス表示、主要な文字コントラスト、モバイル表示を確認し、検出した問題を修正して本番へ反映した。

- 本番 URL: https://sns-agent24.dearsconsulting.com/
- Vercel deployment: `dpl_Guy8CiBsrjUen9aF94QuSjJmSoKL`
- Deployment URL: https://sns-agent24-dearsconsulting-qau3jmult-mros-projects-456975c0.vercel.app
- 本番状態: `READY`
- 本番トップ: HTTP 200
- Vercel 直近 1 時間の runtime error: 0 件

## 導線監査

| 手順 | 画面 | 確認結果 | 状態 |
| --- | --- | --- | --- |
| 1 | `https://dearsconsulting.com/` グローバルメニュー | 旧紹介ページを挟まず、本番サブドメインへ直接移動 | 正常 |
| 2 | `/` | 「無料でログインして開始する」「無料で投稿案を1本つくる」から認証・アプリ導線へ移動 | 正常 |
| 3 | `/app` | 未認証時は `/sign-in?redirect_url=.../app` へ 1 回だけ移動し、認証後は投稿作成画面を表示 | 正常 |
| 4 | `/dashboard` | 未認証時はサインインへ移動し、認証後はアカウント・プラン・利用状況を表示 | 正常 |
| 5 | `/approve` | 未認証時はサインインへ移動し、認証後は承認待ち一覧を表示 | 正常 |
| 6 | `/success` | 未認証時はサインインへ移動。`session_id` がない直接アクセスでは説明付きエラーを表示 | 正常 |
| 7 | `/sign-in` / `/sign-up` | ページを表示し、指定された `redirect_url` を保持 | 正常 |
| 8 | 外部ページ `/sns-agent24/` / `/otoiawase/` | HTTP 200。紹介ページ内 CTA は本番サブドメインへ直接接続 | 正常 |

認証済み確認では、実利用者の契約・決済・Stripe イベントを変更していない。`/approve` では 5 件のカード表示まで確認し、承認・却下・再生成・公開ボタンは実行していない。

## 修正内容

- cron 通知に残っていた旧 Vercel URL `instagram-auto-sigma.vercel.app` を本番 URL に統一。
- 全画面共通の「本文へ移動」スキップリンクを追加。
- トップ、認証、投稿作成、承認、決済完了画面に `main` ランドマークと `#main-content` を設定。
- トップの主要リンク群にナビゲーションラベルを追加。
- 投稿作成・ダッシュボード・承認画面の見出し階層を修正。
- 小さかった戻るリンクと操作ボタンを、おおむね 44px 以上の操作領域へ拡大。
- ボタン・リンクのキーボードフォーカス表示を明確化。
- 読みにくかった薄い文字色を調整し、検出した低コントラスト表示を解消。
- ローディング・処理結果に `status` / `aria-live` を設定。
- 承認カードの画像代替テキストを内容に応じた表現へ変更。
- `prefers-reduced-motion` 設定時にアニメーションを抑制。
- 本番 URL と廃止 URL の混入を検出する `test:links` を追加。

## 本番再監査

### デスクトップ

- トップ: `main` 1、`nav` 1、`header` 1、`footer` 1、`h1` 1。空のリンク名、画像代替テキスト欠落、重複 ID、不正な `href`、危険な `target=_blank`、見出し飛びを検出せず。
- 投稿作成: `main` 1、`h1` 1、`h2` 3、`h3` 8。アカウント状態カードに契約プランと利用権限を分離表示。
- ダッシュボード: `main` 1、`h1` 1、`h2` 3。戻るリンクと主要操作ボタンの操作領域を拡大。
- 承認: 承認待ち 5 件を読み込み、カードごとの `h2` と投稿文の `h3` を設定。
- 決済完了: `main` 1、`h1` 1。`session_id` なしの直接アクセスで説明と戻り先を表示。
- Tab / Shift+Tab: スキップリンクがフォーカス時に表示され、`#main-content` を指すことを確認。
- 計算済みスタイルによる簡易コントラスト検査: トップで検出 0 件。修正前に投稿作成画面で検出した 7 件を修正。

Chrome コンソールには、ブラウザ拡張機能のメッセージチャネル終了に由来する同一形式のエラーだけが記録された。アプリ固有の例外は確認していない。

### モバイル

- 390 × 844 でトップと投稿作成画面を確認。
- カードと操作要素が縦方向へ自然に並び、目視上の横切れ・重なり・操作不能を確認せず。
- 投稿作成画面のアカウント表示で `Freeプラン` と `運営者` が別項目として読めることを確認。

## アカウント表示について

本番で確認したログインユーザーは、SNS Agent24 の契約情報として `Freeプラン・有料契約なし`、利用権限として `運営者` が返されている。ほかの商品・サービスの支払いや、運営権限を SNS Agent24 の有料契約と同一視せず、バックエンドの情報を分けて表示する現行仕様に一致している。

## 自動検証

- `npm run test:links`: 3/3 成功
- `npm run test:account-status`: 6/6 成功
- `npm run test:billing`: 11/11 成功
- `npm run test:copy-safety`: 5/5 成功
- `npm run lint`: 成功
- `npm run build`: 成功
- `npm run test:vercel-trace`: 5 ファイル成功
- 本番 URL と認証対象ルート: 最終 HTTP 200

`npm audit --omit=dev` では Next.js が依存する PostCSS に moderate 2 件が残る。提示された自動修正は Next.js 9.3.3 への破壊的ダウングレードを伴うため実行していない。依存関係側の安全な更新経路が提供された時点で対応する。

## 証跡と制約

画面証跡は同ディレクトリの `01-app.jpg` から `07-mobile-app.jpg` に保存した。ログイン名・メールアドレス等を含むため Git には含めない。

本監査は、主要導線の実ブラウザ確認、HTTP 応答、DOM・計算済みスタイルの簡易検査、キーボードフォーカス、デスクトップと 390px 幅の目視確認を組み合わせたもの。スクリーンリーダー実機、全支援技術、全ブラウザ、全状態を網羅する WCAG 適合性試験ではない。
